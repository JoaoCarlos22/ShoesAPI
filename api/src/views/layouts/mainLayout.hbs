<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/assets/tenis-de-corrida.png"> 
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/styles/{{style}}">
</head>
<body>
    <style>
        /* Estilo geral para a página */
        body {
            font-family: Arial, sans-serif; /* Define a fonte padrão para o corpo do texto */
            margin: 0; /* Remove as margens padrão do corpo */
            padding: 0; /* Remove o preenchimento padrão do corpo */
        }

        /* Estilo para o cabeçalho principal */
        .main-header {
            display: flex; /* Transforma o elemento em um container flexbox */
            justify-content: space-between; /* Distribui os itens de forma a ocupar todo o espaço disponível */
            align-items: center; /* Alinha os itens verticalmente ao centro */
            padding: 20px; /* Adiciona um espaço interno ao cabeçalho */
            background-color: #dad4ce; /* Define a cor de fundo do cabeçalho */
            color: #000000; /* Define a cor do texto do cabeçalho */
        }

        .logo a{
            cursor: pointer;
        }

        /* Estilo para o logo */
        .logo a h1 {
            margin: 0; /* Remove as margens do título */
            font-size: 24px; /* Define o tamanho da fonte do título */
            color: #A68DAD; /* Define a cor do título */
        }

        /* Estilo para o menu do usuário */
        /* Estilo para o menu do usuário */
        .user-menu a {
        color: #130e0e; /* Define a cor dos links do menu */
        margin-left: 15px; /* Adiciona uma margem à esquerda dos links */
        text-decoration: none; /* Remove a sublinhado dos links */
        }

        /* Estilo do menu suspenso */
        .dropdown {
        position: relative; /* Define o posicionamento do dropdown */
        display: inline-block;
        }

        .dropbtn {
        background-color: #651c79; /* Cor de fundo do botão */
        color: #fff; /* Cor do texto */
        padding: 10px 20px;
        font-size: 16px;
        border: none; /* Remove as bordas */
        cursor: pointer;
        border-radius: 5px; /* Borda arredondada */
        }

        .dropbtn:hover {
        background-color: #9c49b3; /* Cor de fundo ao passar o mouse */
        }

        /* Estilo do conteúdo do menu suspenso */
        .dropdown-content {
        display: none; /* Inicialmente oculto */
        position: absolute;
        background-color: #f1f1f1; /* Cor de fundo */
        min-width: 160px; /* Largura mínima */
        box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2); /* Sombra */
        z-index: 1; /* Mantém o dropdown acima de outros elementos */
        border-radius: 5px;
        }

        .dropdown-content a {
        color: #130e0e; /* Cor dos links */
        padding: 12px 16px;
        text-decoration: none; /* Remove o sublinhado */
        display: block; /* Alinha os itens verticalmente */
        }

        .dropdown-content a:hover {
        background-color: #ddd; /* Cor ao passar o mouse nos links */
        }

        /* Mostra o menu suspenso ao passar o mouse */
        .dropdown:hover .dropdown-content {
        display: block; /* Exibe o menu */
        }
        /* Estilo para a barra de navegação central */
        .nav-bar {
            background-color: #A68DAD; /* Define a cor de fundo da barra */
            padding: 10px 20px; /* Adiciona um espaço interno à barra */
        }

        /* Estilo para os links da navegação principal */
        .main-nav a {
            color: #fff; /* Define a cor dos links */
            margin-right: 15px; /* Adiciona uma margem à direita dos links */
            text-decoration: none; /* Remove a sublinhado dos links */
        }

        /* Estilo para o banner promocional */
        .promo-banner {
            background-color:#dad4ce; /* Define a cor de fundo do banner */
            color: #130e0e; /* Define a cor do texto do banner */
            text-align: center; /* Alinha o texto ao centro */
            padding: 5px; /* Adiciona um espaço interno ao banner */
            font-size: 15px; /* Define o tamanho da fonte do texto */
        }
        footer {
            background-color:#A68DAD; /* Cor de fundo */
            color: #130e0e; /* Cor do texto */
            text-align: center;
            padding: 20px;
        }

        footer nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
        }

        footer nav li {
            margin: 0 10px;
        }
        
    </style>


    <!-- Cabeçalho do site -->
    <header>
        <!-- Contêiner principal do cabeçalho -->
        <div class="main-header">
            
            <!-- Título/logo da empresa -->
            <div class="logo">
                <a onclick="window.location.href='/ShoesSystem/home'"><h1>Iannuzzi</h1></a>
                
            </div>

            <!-- Menu de navegação do usuário -->
             <nav class="user-menu">
                <div class="dropdown">
                    <button class="dropbtn">Áreas do Usuário</button>
                    <div class="dropdown-content">
                        <a onclick="window.location.href='/ShoesSystem/home'">Cliente</a>
                        <a onclick="window.location.href='/ShoesSystem/fornecedores'">Fornecedor</a>
                    </div>
                </div>
            </nav>
        </div>
        
        <!-- Barra de navegação principal -->
        <div class="nav-bar">
            <nav class="main-nav">
                <a href="#">Lançamentos</a>
                <a href="#">Masculino</a>
                <a href="#">Feminino</a>
                <a href="#">Ofertas</a>
                <a href="#">Marcas</a>
            </nav>
        </div>
        
        <!-- Banner promocional -->
        <div class="promo-banner">
            <p>Frete grátis para compras acima de R$199,90 | Ganhe <strong>+15% OFF</strong> na primeira compra com o cupom PRIMEIRA15</p>
        </div>
    </header>

    {{{ body }}}

    <footer>
        <p>&copy; 2024 Iannuzzi. Todos os direitos reservados.</p>
        <nav>
            <ul>
                <li><a href="#">Sobre nós</a></li>
                <li><a href="#">Contato</a></li>
                <li><a href="#">Política de privacidade</a></li>
                </ul>
        </nav>
    </footer>
</body>
</html>
